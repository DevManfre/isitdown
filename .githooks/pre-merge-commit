#!/bin/sh
# Block a plain `git merge` that would bring the excluded paths into a branch
# that must not carry them.
#
# Template: installed into .git/hooks/ by scripts/setup-hooks.sh.
script="$(git rev-parse --absolute-git-dir)/merge-clean"
[ -x "$script" ] || exit 0
exec "$script" --guard
